**/__pycache__/
*.pyc
.mcp_models/
.mcp_versions/
.coverage
htmlcov/
tokens.json

# Deployment secrets
.env

# OAuth 2.0 bridge state (issued access/refresh tokens) — see oauth_bridge.py
oauth-state/
# Holds the HMAC key every trained model is signed with. Never committed:
# with it, anyone could hand this server a pickle it would agree to unpickle.
model-keys/

# Default host directory for the shared output mount (docker-compose MCP_SHARED_DIR)
shared-files/
